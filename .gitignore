node_modules
.next
out
.DS_Store
*.log

# Secrets — every machine keeps its own copy; see CLAUDE.md
.env*
!.env.example

# Prisma client is generated from schema.prisma on install
/generated

# Build caches and desktop-environment cruft: pure churn across machines
tsconfig.tsbuildinfo
.directory

# LibreOffice lock files — created while a spreadsheet in utils/ is open
.~lock.*#

# Excel's own lock files, same thing on the machine that runs Office
~$*

# Regenerated by `npm run import:report` on every run, timestamp and all.
utils/import-report.txt
# Same, for `npm run import:desks`.
utils/desk-assignment-report.txt
